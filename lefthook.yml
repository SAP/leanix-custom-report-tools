pre-commit:
  commands:
    format:
      priority: 1
      glob: '*.{ts,js,json,yaml,yml,md}'
      run: npx prettier --write {staged_files}
      stage_fixed: true
    lint:
      priority: 2
      glob: '*.ts'
      run: npx eslint --fix {staged_files}
      stage_fixed: true
